version: 0.1

phases:
  install:
    commands:
      - yum install -y docker
  pre_build:
    commands:
      - /etc/rc.d/rc.cgconfig restart
      - /etc/init.d/docker start
  build:
    commands:
      - mvn clean package
      - docker build -t hello-world .